name: contacts_impl
description: A new Flutter package project.
version: 0.0.1

publish_to: 'none'

environment:
  sdk: ">=2.17.6 <3.0.0"
  flutter: ">=1.17.0"

dependencies:
  flutter:
    sdk: flutter
  flutter_contacts: ^1.1.5
  contacts:
    path: ../../contacts
  persistence:
    path: ../../persistence

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^2.0.0


